diff --git a/McpMod.StateBuilder.cs b/McpMod.StateBuilder.cs
index 76eee84..29b1892 100644
--- a/McpMod.StateBuilder.cs
+++ b/McpMod.StateBuilder.cs
@@ -1073,6 +1073,17 @@ public static partial class McpMod
         battle["round"] = combatState.RoundNumber;
         battle["turn"] = combatState.CurrentSide.ToString().ToLower();
         battle["is_play_phase"] = IsPlayPhase(combatState);
+        var runManager = RunManager.Instance;
+        bool executorIdle = runManager.ActionExecutor?.CurrentlyRunningAction == null;
+        bool queueEmpty = runManager.ActionQueueSet?.IsEmpty ?? false;
+        battle["action_running"] = !executorIdle;
+        battle["action_queue_empty"] = queueEmpty;
+        var handUi = NCombatRoom.Instance?.Ui?.Hand;
+        battle["ready_for_action"] = IsPlayPhase(combatState)
+            && executorIdle && queueEmpty
+            && !CombatManager.Instance.PlayerActionsDisabled
+            && handUi != null && !handUi.InCardPlay
+            && handUi.CurrentMode == NPlayerHand.Mode.Play;
 
         // Enemies
         var enemies = new List<Dictionary<string, object?>>();
@@ -1911,6 +1922,9 @@ public static partial class McpMod
 
         // Cards in the grid (sorted by visual position - MoveToFront can reorder children)
         var cardHolders = FindAllSortedByPosition<NGridCardHolder>(screen);
+        var selectedCards = GetInstanceFieldValue(screen, "_selectedCards") as HashSet<CardModel>;
+        state["selection_observable"] = selectedCards != null;
+        var selectedIndices = new List<int>();
         var cards = new List<Dictionary<string, object?>>();
         int index = 0;
         foreach (var holder in cardHolders)
@@ -1920,10 +1934,21 @@ public static partial class McpMod
 
             var cardInfo = BuildCardInfo(card);
             cardInfo["index"] = index;
+            if (selectedCards != null)
+            {
+                bool selected = selectedCards.Contains(card);
+                cardInfo["is_selected"] = selected;
+                if (selected) selectedIndices.Add(index);
+            }
             cards.Add(cardInfo);
             index++;
         }
         state["cards"] = cards;
+        if (selectedCards != null)
+        {
+            state["selected_count"] = selectedCards.Count;
+            state["selected_indices"] = selectedIndices;
+        }
 
         // Preview container showing? (selection complete, awaiting confirm)
         // Upgrade screens use UpgradeSinglePreviewContainer / UpgradeMultiPreviewContainer
diff --git a/McpMod.cs b/McpMod.cs
index 3b5ed64..f4a4c7d 100644
--- a/McpMod.cs
+++ b/McpMod.cs
@@ -83,7 +83,10 @@ public static partial class McpMod
         try
         {
             // Optional settings UI patches should not block the HTTP bridge itself.
-            TryApplyHarmonyPatches();
+            // The optional speed-settings detours stall startup on this macOS
+            // host. The REST bridge does not depend on them.
+            if (!OperatingSystem.IsMacOS()) TryApplyHarmonyPatches();
+            GD.Print("[STS2 MCP] Initializing REST bridge (macOS: no settings detours)");
 
             // Connect to main thread process frame for action execution
             var tree = (SceneTree)Engine.GetMainLoop();
@@ -178,9 +181,18 @@ public static partial class McpMod
         {
             var request = context.Request;
             var response = context.Response;
-            response.Headers.Add("Access-Control-Allow-Origin", "*");
-            response.Headers.Add("Access-Control-Allow-Methods", "GET, POST, OPTIONS");
-            response.Headers.Add("Access-Control-Allow-Headers", "Content-Type");
+            // This bridge is for the local controller, never browser pages.
+            if (!string.IsNullOrEmpty(request.Headers["Origin"]))
+            {
+                SendError(response, 403, "Browser origins are not permitted");
+                return;
+            }
+            if (request.HttpMethod == "POST"
+                && !(request.ContentType?.StartsWith("application/json", StringComparison.OrdinalIgnoreCase) ?? false))
+            {
+                SendError(response, 415, "application/json required");
+                return;
+            }
 
             if (request.HttpMethod == "OPTIONS")
             {
